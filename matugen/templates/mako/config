# Generated by matugen (mango-adapted). Overwritten on every wallpaper switch.
# Matches the waybar pill spec: surface_container_low + 12px radius + Google Sans Flex.

font=Google Sans Flex 11
background-color={{colors.surface_container_low.default.hex}}
text-color={{colors.on_surface.default.hex}}
border-color={{colors.outline_variant.default.hex}}
progress-color=over {{colors.primary.default.hex}}
border-size=1
border-radius=12
padding=12,16
margin=8
outer-margin=47,10,0,0
width=400
height=160
icon-border-radius=8
max-icon-size=48
max-visible=4
group-by=app-name
layer=overlay

# Auto-dismiss. ignore-timeout=1 is required: apps that send expire_timeout=0
# mean "never expire" per the spec and would otherwise ignore default-timeout.
default-timeout=5000
ignore-timeout=1

# Left click = focus the kitty window that sent it (mako's own default,
# stated explicitly so a future edit doesn't silently reintroduce dismiss).
on-button-left=invoke-default-action
on-button-right=dismiss
on-button-middle=none
# on-touch (swipe) defaults to dismiss; left unset intentionally.

[urgency=low]
default-timeout=3000
text-color={{colors.on_surface_variant.default.hex}}

[urgency=critical]
# The only notifications that stay: persist until clicked, error-colored.
default-timeout=0
ignore-timeout=1
border-color={{colors.error.default.hex}}
text-color={{colors.on_surface.default.hex}}
border-size=2

# Claude Code's generic "idle, nothing pending" ping — as opposed to an
# actual permission/plan/question prompt, which still gets the default style.
[app-name=kitty summary="Claude Code" body="Claude is waiting for your input"]
default-timeout=3000
border-color={{colors.term2.default.hex}}
text-color={{colors.term2.default.hex}}
